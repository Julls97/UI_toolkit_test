/* Светлая тема */

.background {
    background-color: #ffffff;
}

.background-subtle {
    background-color: #FAFBFE;
}

.foreground {
    color: #1a1a1a;
}

.card {
    background-color: #ffffff;
    color: #1a1a1a;
    border-radius: 12px;
    padding: 20px;
    margin: 10px;
    width: 300px;
    min-width: 300px;
    max-width: 300px;
    box-shadow: 0 10px 15px rgba(0,0,0,0.1);
}

.card-title {
    font-size: 20px;
    font-weight: bold;
    margin-bottom: 10px;
    color: #1a1a1a;
    word-wrap: break-word;
    overflow-wrap: break-word;
}

.card-description {
    font-size: 16px;
    line-height: 1.5;
    margin-bottom: 15px;
    color: #4a5568;
    white-space: pre-wrap;
    word-wrap: break-word;
    overflow-wrap: break-word;
}

.card-button {
    padding: 10px 20px;
    border-radius: 8px;
    border: none;
    font-weight: bold;
    cursor: pointer;
    transition: all 0.2s ease;
    min-width: 120px;
}

/* Основные акценты */
.primary {
    background-color: #4f46e5;
    color: #ffffff;
}
.primary-text {
    color: #4f46e5;
}

.secondary {
    background-color: #f1f5f9;
    color: #334155;
    border: 1px solid #e2e8f0;
}

/* Тёплые фирменные цвета */
.brand-warm-50 { background-color: #fff7ed; }
.brand-warm-100 { background-color: #ffedd5; }
.brand-warm-200 { background-color: #fed7aa; }
.brand-warm-300 { background-color: #fdba74; }
.brand-warm-400 { background-color: #fb923c; }
.brand-warm-500 { background-color: #f97316; }
.brand-warm-600 { background-color: #ea580c; }
.brand-warm-700 { background-color: #c2410c; }
.brand-warm-800 { background-color: #9a3412; }
.brand-warm-900 { background-color: #7c2d12; }
.brand-warm-950 { background-color: #431407; }

/* Эффекты наведения для кнопок */
.card-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}

.card-button.primary:hover {
    background-color: #4338ca;
}

.card-button.secondary:hover {
    background-color: #e2e8f0;
}

.card-button.brand-warm-600:hover {
    background-color: #c2410c;
}
